import { ProyectFrontFrame } from "../StyledComponents/Proyects/ProyectFrontFrame.Styled";
const ProyectFrame = ( { align } ) => {
    return(
        <ProyectFrontFrame align={ align }>
        </ProyectFrontFrame>
    );
};

export default ProyectFrame;